{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --no-audit --no-fund --legacy-peer-deps && npx medusa build && cd .medusa/server && npm install --no-audit --no-fund --legacy-peer-deps"
  },
  "deploy": {
    "startCommand": "cd .medusa/server && npx medusa db:migrate && npx medusa start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
